# gp-sphinx-astro-builder

Sphinx builder that walks each doctree and emits typed JSON validated by
Pydantic models. The output is consumed by an Astro static site through
its standard content collections.

See `notes/plans/astro.md` (in the workspace root) for the full design.
