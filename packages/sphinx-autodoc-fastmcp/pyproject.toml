[project]
name = "sphinx-autodoc-fastmcp"
version = "0.0.1a35"
description = "Sphinx extension for documenting FastMCP tools (cards, badges, cross-refs)"
requires-python = ">=3.10,<4.0"
authors = [
  {name = "Tony Narlock", email = "tony@git-pull.com"}
]
license = { text = "MIT" }
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: MIT License",
  "Framework :: Sphinx",
  "Framework :: Sphinx :: Extension",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Documentation",
  "Topic :: Documentation :: Sphinx",
  "Typing :: Typed",
]
readme = "README.md"
keywords = ["sphinx", "fastmcp", "mcp", "documentation", "badges"]
dependencies = [
  "sphinx>=8.1",
  "sphinx-ux-badges==0.0.1a35",
  "sphinx-ux-autodoc-layout==0.0.1a35",
  "sphinx-autodoc-typehints-gp==0.0.1a35",
]

[project.urls]
Repository = "https://github.com/git-pull/gp-sphinx"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sphinx_autodoc_fastmcp"]

[tool.gp-sphinx.docs]
# fastmcp registers the richest surface in the autodoc family —
# seven directives plus eight roles. The kitchen-sink page exercises
# every one on a single page so contributors can see at a glance
# what the extension exposes.
showcase = ["kitchen-sink"]
