gp-sphinx
=========

A Sphinx documentation platform for Python projects.

This site documents ``gp_sphinx`` *itself* — the very package whose
``merge_sphinx_config`` helper consolidates the shared Sphinx defaults
that fourteen sibling ``sphinx-*`` packages plug into.

.. autofunction:: gp_sphinx.config.merge_sphinx_config
